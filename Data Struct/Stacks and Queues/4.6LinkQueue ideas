一、建立一个队列的结点和队列的链表
1.队列结点中包含数据和指向下一个结点的指针
2.队列的链表结构体中包含队首和队尾结点

二、队列的初始化（构造空队列）
1.为头结点和队尾结点申请内存；--（注意此处不是队头结点！注意区分）
2.如果没有申请到（地址为空），则提示；
3.若申请到空间了，另队头结点的下一个结点（队头结点）为空

三、插入元素e为Q的新的队尾元素
1.为待入队的数据e申请一个新结点s内存，申请失败则返回提示；
2.将数据赋值给s的数据域，空地址赋值给s的后继指针域；
3.将新结点s赋值给原来队尾元素的后继指针域；
4.更新队尾结点为s

四、若队列不空,则打印Q的队头元素,否则打印提示
1.判断队列是否为空（若头结点==队尾结点，队列为空），空则打印提示；
2.打印头结点的下一个结点（队头结点）的数据域

五、求队列的长度
1.声明一个结点p，指向队头，声明一个计数变量i；
2.当队头不等于队尾时，计数变量加一，p指向下一个地址；
3.打印队列的长度；

六、从队头到队尾依次对队列Q中每个元素输出 
1.声明一个临时结点p，指向头结点的下一个结点（队头结点）；
2.当结点不空时，输出该节点的数据域，并将p指向下一个结点

七、若队列不空,删除Q的队头元素,打印其值,否则打印提示
1.判断队列是否为空（若头结点==队尾结点，队列为空），空则打印提示；
2.声明一个临时结点p，将p初始化为头结点的下一个结点（队头结点），即：将欲删除的队头结点暂存给p
3.打印p的数据域；
4.将原队头结点的后继p->next赋值给头结点的后继；
5.如果队头就是队尾(队列仅一个元素)，将队尾指向头结点
6.释放临时结点p

八、清空队列
1.声明临时变量p，q；
2.令队尾指向队头；
3.令p暂存队头结点；
4.清空队头结点；
5.当p不空时，将p先给q，在将p指向下一个结点，之后释放q；

九、销毁队列
1.当头结点不空时进入循环；
2.将队尾结点指向队头结点；
3.释放队头节点；
4.将头结点指向队尾结点；
