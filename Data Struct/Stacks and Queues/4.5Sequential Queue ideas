//循环队列的顺序存储结构

一、定义一个队列结构体--队列先进先出，头出尾进
1.包含一个数组用于存储数据，
2.包含两个指针(在数组中即为下标)，一个指向队列的头，一个指向队列的尾

二、初始化一个空队列（请空队列）
1.令队头和队尾都为零

三、若队列未满，则插入元素e为Q新的队尾元素
1.判断队列是否已满(依据公式：(尾+1)/队列长取余=头)，满了则提示
2.没满，则把数据给队尾处（队尾之前未存储数据）
3.队尾向后移动一位（继续指向一个空位），防止假溢出，除队列长取余。

四、输出队列的长度
1.直接利用队列长度的计算公式：(尾-头+队列长)除队列长取余

五、从队头到队尾依次对队列Q中每个元素输出 
1.当对头不等于队尾，输出当前的值（循环遍历）

六、若队列不空，则删除Q中队头元素，用e返回其值
1.判断队列是否为空（头等于尾），空则提示；
2.队列不空，打印头结点数据
3.头结点位置下移一个，防止假溢出，除队列长取余。


