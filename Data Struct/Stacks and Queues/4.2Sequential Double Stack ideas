一、建立一个双栈结构体
1.建立一个结构体，包括一个数组，两个栈顶指针，表示两栈共享空间结构；

二、双栈的初始化（设置空栈）
1.令一个栈顶指针指向数组最左段元素的前一个，一个指向最右端元素的后一个！

三、插入元素e为新的栈顶元素
1.判断是否栈满；
2.判断是入左面的栈还是右面的栈；
3.栈顶指针偏移一个位置，在新位置数据进栈

四、返回栈的长度
1.两个指针相对于空栈时的指针的偏移量绝对值之和！

五、从前往后输出栈中元素的数据
1.先输出左面栈的全部元素，建立循环，直到栈顶，输出每个元素
2.再输出右面栈的全部元素，建立循环，直到栈顶，输出每个元素（注意循环条件不同）

六、若栈不空，则删除S的栈顶元素，打印删除的值
1.判断要删处哪一个栈的元素；
2.判断该栈是否空，空则提示；
3.栈非空，将栈顶元素输出，栈顶指针减一；
***注意插入和删除元素时的不同：
   i++ : 先在i所在的表达式中使用i的当前值，再让i加1
   ++i : 先让i加1，再在i所在的表达式中使用i的新值。
