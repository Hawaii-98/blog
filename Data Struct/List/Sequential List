//创建一个线性表结构，并进行初始化操作
//思路如下：
  一、创建一个线性表结构
  1.定义一个结构体；
  2.结构体中包含一个数组，用于存储数据；
  3.结构体中包含一个线性表长度变量，与数组长度区分开。
  
  二、初始化线性表
  1.令线性表的长度为零；
  2.返回某一特定标记值，确认是否初始化完成。

-----------------------------------------<我是分割线>-------------------------------------------
代码实现：

#include<stdio.h>

#define MAX 20

//结构体定义
typedef struct 
{
    int data[MAX];
    int length;
}sqList;

//初始化线性表
int InitList (sqList *L)
{
    L->length=0;
    return 666;
}

main()
{
   int flag;
   sqList L;

   flag=InitList(&L);
   printf("初始化线性表之后的线性表长度是%d\n",L.length);
   printf("线性表初始化真的%d\n",flag);
}
