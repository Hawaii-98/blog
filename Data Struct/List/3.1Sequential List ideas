#线性表顺序存储结构的操作思路
  一、创建一个线性表结构
  1.定义一个结构体；
  2.结构体中包含一个数组，用于存储数据；
  3.结构体中包含一个线性表长度变量，与数组长度区分开。
  
  二、初始化线性表
  1.令线性表的长度为零；
  2.返回某一特定标记值，确认是否初始化完成。
  
  三、向线性表中插入一个元素
  1.检查顺序表是否已满；
  2.检查插入位置是否符合要求（当i比第一位置小或者比最后一位置后一位置还要大时--错误）
  3.若插入数据位置不在表尾, 将要插入位置之后的数据元素向后移动一位 
  4.插入新元素
  5.线性表长度加一
	
